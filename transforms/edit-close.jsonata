($val := $trim($.value) ; $val  
    ? [$map($cell, function($t){ ($t.id = $find("li|").id) ? $sift($merge([$t, {"title": $t.esc ? $t.esc : $val, "editing": null, "esc": null}]), function($v) { $v != null }) : $t })] 
    : [$filter($cell, function($t){ $t.id != $find("li|").id })] )